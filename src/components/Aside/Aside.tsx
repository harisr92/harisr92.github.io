import React, { useState } from "react";
import { Link, withPrefix, PageProps } from "gatsby"


const Aside = (): React.FC<PageProps> => {
  return (
    <div className="sidebar-wrapper sidebar-wrapper-right">
    </div>
  )
};

export default Aside;
